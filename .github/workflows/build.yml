name: 打包

on:
  push:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: windows-latest
    steps:
#      - name: 拉取代码
#        uses: actions/checkout@v4
#      - name: 安装环境
#        uses: actions/setup-python@master
#        with:
#          python-version: 3.12
#      - name: 安装依赖
#        run: |
#          pip3 install -r requirements.txt
      - name: 安装编译环境
        uses: egor-tensin/setup-mingw@v2
        with:
          version: 14.2.0
          platform: x86_64
          thread: posix
          exception: seh
          runtime: msvcrt
          revision: 2
          install-dir: C:/tools/mingw64
#          location: %localappdata%\Nuitka\Nuitka\Cache\downloads\gcc\x86_64
      - name: 验证版本
        run: |
          C:/tools/mingw64/bin/gcc --version
          C:/tools/mingw64/bin/g++ --version
      - name: 查看目录
        shell: cmd
        run: |
          cd /d D:\
          dir
#      - name: 开始打包
#        shell: pwsh
#        run: |
#          (chcp 936) -and (echo "Yes" | .\package.bat)
#      - name: 上传文件
#        uses: actions/upload-artifact@v4
#        with:
#          name: windows
#          path: |
#            D:\Downloads\*.exe
