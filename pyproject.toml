[project]
name = "mole"
version = "1.9.3"
description = """
- 优化：餐厅自动做菜执行时增加在标题显示要做的菜名、最近一次做菜倒计时等信息
- 新增：增加通用的标题栏功能信息提示方法
"""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cffi~=2.1.0",
    "nuitka~=4.1.3",
    "packaging~=26.2",
    "py3amf~=0.9.1",
    "pypinyin~=0.55.0",
    "pyside6~=6.11.1",
    "requests~=2.34.2",
    "zstandard~=0.25",
]
