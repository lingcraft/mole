[project]
name = "mole"
version = "1.12.0"
description = """
- 新增：新增泡泡龙游戏一键通关功能，在进入游戏后开启功能，Bot 会自动计算并通关当前关卡，进入下一关需用户手动点击
"""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cffi~=2.1.0",
    "loguru~=0.7.3",
    "nuitka~=4.1.3",
    "packaging~=26.2",
    "py3amf~=0.9.1",
    "pypinyin~=0.55.0",
    "pyside6~=6.11.1",
    "requests~=2.34.2",
    "zstandard~=0.25",
]
