[project]
name = "mole"
version = "1.10.5"
description = """
- 修复：餐厅做菜若0到6点启动，下次做菜时间为6点整，现在改为6点5分，尝试解决6点准时的话会被强制断开连接从而做菜失败的问题
"""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cffi~=2.1.0",
    "nuitka~=4.1.3",
    "packaging~=26.2",
    "py3amf~=0.9.1",
    "pypinyin~=0.55.0",
    "pyside6~=6.11.1",
    "requests~=2.34.2",
    "zstandard~=0.25",
]
