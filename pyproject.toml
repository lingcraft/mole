[project]
name = "mole"
version = "1.9.6"
description = """
- 修复：修复多次开始再停止自动做菜后，定时器信号连接了多个函数导致计算下次做菜倒计时异常的问题
"""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cffi~=2.1.0",
    "nuitka~=4.1.3",
    "packaging~=26.2",
    "py3amf~=0.9.1",
    "pypinyin~=0.55.0",
    "pyside6~=6.11.1",
    "requests~=2.34.2",
    "zstandard~=0.25",
]
