[project]
name = "mole"
version = "1.7.20"
description = """
- 修复：修复当按顺序操作："清空封包、点击任意按钮发送封包、切换功能区域页面"时，会自动触发封包列表区域的 currentCellChanged 信号导致发送区域自动显示 ip 的问题
"""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cffi~=2.0.0",
    "nuitka~=4.1.3",
    "packaging~=26.2",
    "pypinyin~=0.55.0",
    "pyside6~=6.11.1",
    "requests~=2.34.2",
]
