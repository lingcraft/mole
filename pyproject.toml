[project]
name = "mole"
version = "1.7.15"
description = """
- 优化：增加 run_later_expect 函数，在等待到期望的包之后再执行相应功能，如果需要结果，会将结果传递给功能函数
- 优化：元素骑士开始前查询无尽深渊和莎士摩亚已挑战次数，查询到之后再开始执行，但目前暂时不处理数据，不知道已挑战次数是到副本开始时间后刷新还是不刷新，如果是不刷新，并且在开始后挑战一次才刷新，那就没有参考意义了
"""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cffi~=2.0.0",
    "nuitka~=4.1.3",
    "packaging~=26.2",
    "pypinyin~=0.55.0",
    "pyside6~=6.11.1",
    "requests~=2.34.2",
]
